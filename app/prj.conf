# TF-M's "profile not set" build (see CONFIG_TFM_PROFILE_TYPE_NOT_SET in this
# board's conf) unconditionally enables TFM_EXPERIMENTAL
CONFIG_WARN_EXPERIMENTAL=n

# Set LIBC
CONFIG_PICOLIBC=y

# PicolibC targets C17 and POSIX 2018 standards
CONFIG_STD_C17=y

# Enforce MISRA C standards
CONFIG_MISRA_SANE=y

# Enable FPU & FPU sharing
CONFIG_FPU=y
CONFIG_FPU_SHARING=y

# Reset Reason and Reboot
CONFIG_HWINFO=y
CONFIG_REBOOT=y

# Power Management
CONFIG_PM_DEVICE=y

# Compiler optimizations
# Optimize for speed since space isn't a huge concern for this app
CONFIG_SPEED_OPTIMIZATIONS=y

# Flash operations.
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_FLASH_PAGE_LAYOUT=y
CONFIG_STREAM_FLASH=y
CONFIG_SETTINGS=y

# Watchdog timer
CONFIG_WATCHDOG=y
CONFIG_WDT_DISABLE_AT_BOOT=n

# Multiplexor drivers
CONFIG_GPIO=y
CONFIG_MULTIPLEXER=y

# Counter
CONFIG_COUNTER=y

# I2C
CONFIG_I2C=y

# PWM
CONFIG_PWM=y

# Networking
CONFIG_NETWORKING=y
CONFIG_NET_BUF=y
CONFIG_NET_SOCKETS=y
CONFIG_NET_IPV4=y
CONFIG_NET_IPV6=y
CONFIG_NET_UDP=n

# SNTP
CONFIG_SNTP=y

# Stack and heap configurations
# The routes JSON itself lives in static buffers (update_stop.c's json_buf,
# sized by CONFIG_STOP_JSON_BUF_SIZE) -- main's stack mostly carries the
# Swiftly request's native-TLS/mbedTLS call frames. 16384 is 2x the
# TLS-proven 8192 precedent (pigeon-examples' wifi_init); the previous
# 24576 dated from "we have plenty of memory", which stopped being true
# once the pigeon client thread and its buffers moved in (RAM ~94%).
CONFIG_MAIN_STACK_SIZE=16384
# Increase heap size for networking operations
CONFIG_HEAP_MEM_POOL_SIZE=4096

# MCUBOOT
CONFIG_BOOTLOADER_MCUBOOT=y
CONFIG_MCUBOOT_BOOTUTIL_LIB=y

# Enable MCUmgr and dependencies.
CONFIG_ZCBOR=y
CONFIG_MCUMGR=y
CONFIG_BASE64=y
CONFIG_CRC=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_UART_CONSOLE_MCUMGR=y
CONFIG_UART_MCUMGR=y
CONFIG_MCUMGR_TRANSPORT_UART=y

# Increase MCUmgr transfer speeds
CONFIG_UART_MCUMGR_RX_BUF_SIZE=512
CONFIG_MCUMGR_TRANSPORT_NETBUF_SIZE=512

# Some MCUmgr command handlers require a large stack.
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=4096

# Enable most MCUmgr core commands.
CONFIG_IMG_MANAGER=y
CONFIG_IMG_ERASE_PROGRESSIVELY=y
CONFIG_MCUMGR_GRP_IMG=y
CONFIG_MCUMGR_GRP_OS=y
CONFIG_IMG_ENABLE_IMAGE_CHECK=y

# Hardening
CONFIG_FORTIFY_SOURCE_COMPILE_TIME=y
CONFIG_STACK_SENTINEL=y
CONFIG_OVERRIDE_FRAME_POINTER_DEFAULT=y
CONFIG_STACK_CANARIES=y
CONFIG_STACK_POINTER_RANDOM=100

# NCS boot banner
CONFIG_NCS_APPLICATION_BOOT_BANNER_STRING="Embedded Departure Board"

# Logging
CONFIG_LOG=y
# 1 = Error, 2 = Warn, 3 = Info, 4 = Debug
CONFIG_LOG_DEFAULT_LEVEL=2

# Log mode minimal & immediate are good for debugging
CONFIG_LOG_MODE_MINIMAL=y
# CONFIG_LOG_MODE_IMMEDIATE=y

# Log mode deferred
# CONFIG_LOG_MODE_DEFERRED=y
# CONFIG_LOG_SPEED=y
# CONFIG_LOG_DBG_COLOR_BLUE=y
# CONFIG_LOG_PROCESS_THREAD_STACK_SIZE=2048
# The default stack size of 1024 seems okay for log level 1 and 2, but not 3 and 4.
# We have enough unused RAM and I'd rather avoid any potential logging stack overflows either way.

# Log RTT
# CONFIG_LOG_BACKEND_UART=n
# CONFIG_LOG_MODE_MINIMAL=n
# CONFIG_USE_SEGGER_RTT=y
# CONFIG_LOG_BACKEND_RTT=y
# CONFIG_SEGGER_RTT_MODE_NO_BLOCK_TRIM=y

# Network Logging
# CONFIG_NET_LOG=y
# CONFIG_NET_SOCKETS_LOG_LEVEL_DBG=y

# Debugging
CONFIG_DEBUG=y
CONFIG_FAULT_DUMP=2
# CONFIG_DEBUG_COREDUMP=y
# CONFIG_DEBUG_COREDUMP_BACKEND_LOGGING=y
# CONFIG_DEBUG_COREDUMP_MEMORY_DUMP_MIN=y
# CONFIG_DEBUG_OPTIMIZATIONS=y
# CONFIG_THREAD_NAME=y
# CONFIG_THREAD_MONITOR=y
# CONFIG_THREAD_ANALYZER=y
# CONFIG_THREAD_ANALYZER_USE_LOG=y
# CONFIG_THREAD_ANALYZER_USE_PRINTK=y
# CONFIG_THREAD_ANALYZER_AUTO=y
# CONFIG_THREAD_ANALYZER_AUTO_INTERVAL=5
# CONFIG_DEBUG_THREAD_INFO=y
# CONFIG_THREAD_STACK_INFO=y
# CONFIG_MULTIPLEXER_LOG_LEVEL_DBG=y

# CONFIG_SYS_HEAP_LISTENER=y
# CONFIG_SYS_HEAP_RUNTIME_STATS=y
# CONFIG_STACK_USAGE=y
# CONFIG_INIT_STACKS=y

# Pigeon (PidgeIoT device management, see app/west.yml + net/pigeon_client.c)
# HTTPS connector: this sign's day job (the Swiftly fetch loop) is already
# plain HTTPS over the same LTE link, so no protocol/socket-type conflict.
# CONFIG_PIGEON_ENDPOINT/CONFIG_PIGEON_TOKEN are real device secrets --
# supplied via the gitignored prj.local.conf (see this file's CMakeLists.txt
# handling), never committed here.
CONFIG_PIGEON=y
CONFIG_PIGEON_CONNECTOR_HTTPS=y
# Sec tag 2: 0=NO_SEC_TAG, 1=SWIFTLY_SEC_TAG (see net/lte_manager.h's enum
# tls_sec_tags) -- the CA cert provisioned under it is GTS Root R4
# (net/lte_manager.c), since api.pidgeiot.com sits behind the Google Trust
# Services chain. Provisioned into the MODEM's credential store
# (CONFIG_MODEM_KEY_MGMT, board conf): pigeon's sockets are plain offloaded
# IPPROTO_TLS_1_2, so their TLS runs inside the modem -- deliberately NOT
# sharing Swiftly's native mbedTLS path, whose 40KB heap is sized for one
# connection at a time (board conf) and would make concurrent pigeon and
# Swiftly handshakes fight over it.
CONFIG_PIGEON_HTTPS_SEC_TAG=2
CONFIG_PIGEON_LOG_LEVEL=4
# CONFIG_PIGEON_REBOOT_ON_FATAL/_WATCHDOG deliberately NOT enabled here --
# see prj_release.conf's comment: this is the dev/debug profile, and
# pigeon's own Kconfig help explicitly recommends leaving reboot-on-fatal
# off while actively debugging on a dev board.
